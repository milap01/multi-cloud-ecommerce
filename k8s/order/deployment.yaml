apiVersion: apps/v1
kind: Deployment
metadata:
  name: order
  namespace: mce
  labels:
    app: order
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order
  template:
    metadata:
      labels:
        app: order
    spec:
      containers:
        - name: order

          image: 343938549827.dkr.ecr.us-east-1.amazonaws.com/order-repo-mce-2:latest

          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          env:
            - name: PGSSLMODE
              value: "require"
            - name: SERVICE_NAME
              value: "order"
            - name: SERVICE_NAME
              value: "order"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: RDS_HOST
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: mce-secrets
                  key: POSTGRES_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: mce-secrets
                  key: POSTGRES_PASSWORD
            - name: DB_NAME
              value: "ecommerce"
            - name: PRODUCT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: PRODUCT_SERVICE_URL
            - name: KAFKA_BOOTSTRAP
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: KAFKA_BOOTSTRAP

