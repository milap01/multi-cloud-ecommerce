apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: product
  namespace: observability
  labels:
    team: mce
spec:
  selector:
    matchLabels:
      app: product
  namespaceSelector:
    matchNames:
      - mce
  endpoints:
    - port: http
      path: /metrics
      interval: 10s