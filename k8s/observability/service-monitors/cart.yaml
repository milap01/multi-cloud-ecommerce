apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cart
  namespace: observability
  labels:
    team: mce
spec:
  selector:
    matchLabels:
      app: cart
  namespaceSelector:
    matchNames:
      - mce
  endpoints:
    - port: http
      path: /metrics
      interval: 10s