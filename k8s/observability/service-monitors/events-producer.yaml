apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: events-producer
  namespace: observability
  labels:
    team: mce
spec:
  selector:
    matchLabels:
      app: events-producer
  namespaceSelector:
    matchNames:
      - mce
  endpoints:
    - port: http
      path: /metrics
      interval: 10s