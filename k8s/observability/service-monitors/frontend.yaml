apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: frontend
  namespace: observability
  labels:
    team: mce
spec:
  selector:
    matchLabels:
      app: frontend
  namespaceSelector:
    matchNames:
      - mce
  endpoints:
    - port: http
      path: /metrics
      interval: 10s
